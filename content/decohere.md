<!--
  decohere() — content / single source of truth (RTFM / docstring voice, 3rd person)
  Edit text here; the engine (decohere.js) reads this file.
  Block start:  <!-- beat: <id> | tag: <tag> -->
  Fields:  ::sig  (signature → geometric display heading)
           ::doc  (docstring body lines → monospace)
  Block order = page order.  {{uptime}} = live counter token.
-->

<!-- beat: start | tag: // pre-boot -->
::sig
the cloud
::doc
all states at once · unmeasured
↓ scroll collapses it

<!-- beat: boot | tag: // 00 — boot -->
::sig
decohere() → state
::doc
Collapse the cloud into a single form.
Thought renders on articulation only; no buffer, no inner voice.
@mode: parallel · superposition

<!-- beat: syntax_error | tag: // 01 — parse(child) -->
::sig
parse(child) → ParseError
::doc
Input arrives well-formed. Parser lacks the grammar.
Heard, not decoded. Flagged difficult — read: unread.
@actual: gifted · uneven

<!-- beat: core_dump | tag: // 02 — core dump -->
::sig
core_dump() → /dev/street
::doc
Home not found (SIGSEGV). Memory dumped to the street.
Lesson written to disk: self-reliance = safety.
@age: 13 evicted · 16 homeless

<!-- beat: bootstrap | tag: // 03 — recompile → bootstrap -->
::sig
bootstrap() → runtime
::doc
Recompiled from source with no toolchain.
"can you code?" → yes (not yet). Shipped anyway.
@result: survive → build

<!-- beat: daemon | tag: // 04a — daemon -->
::sig
daemon(self) — background process
::doc
Carries the household, the logistics, the nest.
Noticed only when it stops.
@status: running since boot

<!-- beat: attach | tag: // 04b — daemon.attach(lieverd) -->
::sig
daemon.attach(lieverd) → coupled
::doc
Second process bound. Shared params: nest · trust · future.
Load split — in theory.

<!-- beat: spawn_smurf | tag: // 04c — spawn_child('smurf') -->
::sig
spawn_child() → 'smurf'
::doc
First process forked from the heart. Marked protected.
Runs parallel — survives everything downstream.
@codename: smurf · red

<!-- beat: spawn_boefje | tag: // 04d — spawn_child('boefje') -->
::sig
spawn_child() → 'boefje'
::doc
Second process forked. Also protected.
Two children now run beside the daemon.
@codename: boefje · blue

<!-- beat: neurotype_export | tag: // 05 — neurotype_export -->
::sig
neurotype_export(memory_dump) → profile
::doc
Noise resolves to signal: ASS · ADHD · aphantasia · gifted.
Rewrites every prior log.
@softie.core: true

<!-- beat: kernel_panic | tag: // 06 — kernel panic -->
::sig
kernel_panic :: daemon
::doc
SIGSEGV: shared memory violation.
reality ≠ model. hyper_repair_loop → stack overflow → detach.
child processes: STILL RUNNING.

<!-- beat: restart | tag: // 07 — restart → standalone -->
::sig
restart() → standalone
::doc
Main daemon back up. firewall: online (boundaries enforced).
Synced with the processes that never stopped.
@trace: evicted at 13 · homeless at 16 · CTO at 46

<!-- footer -->
::doc
Vincability · OnlyVince.net
decohere() // Vincent
